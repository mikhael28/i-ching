import styled from 'styled-components/macro';

export const P = styled.p`
  font-size: 1.25rem;
  line-height: 1.5;
  color: ${p => p.theme.textSecondary};
  margin: 0.625rem 6px 0.5rem 6px;
  text-align: center;
`;
